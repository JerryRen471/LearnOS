import { EmptyState } from "@/components/ui/states";

export default function MasteryPage() {
  return (
    <section className="page">
      <header className="page-header">
        <h2>Mastery</h2>
        <p>Mastery map page skeleton is ready for data integration.</p>
      </header>
      <EmptyState message="Mastery content will be connected in subsequent issues." />
    </section>
  );
}
